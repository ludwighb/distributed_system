1. Map tasks: 
    a. Assign just one map task to worker. 
    b. assign all map task to worker.
    c. handle concurrency and task failure. 
    d. Assign just one reduce task to worker.
    e. assign all reduce task to worker.
    f. handle concurrency and task failure. 
